#ifndef CONSTANTS_H
#define CONSTANTS_H
typedef enum MOTOR_STATE {
    ON = 1,
    OFF = 0,
} MOTOR_STATE;
typedef enum PANEL {
    STATS = 0,
    HOME = 1,
    SETTINGS = 2,
} PANEL;
#endif // CONSTANTS_H
